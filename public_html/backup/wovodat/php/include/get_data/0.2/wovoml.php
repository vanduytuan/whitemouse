<?php
/*
// Loop on elements - Search for children
$wovoml_elements=$xml_array[0]['value'];
foreach ($wovoml_elements as $wovoml_element) {
	
	// Loading info
	if ($wovoml_element['tag']=="LOADINGINFO") {
		include $include_root."loadinginfo.php";
		continue;
	}
	
	// Observation
	if ($wovoml_element['tag']=="OBSERVATION") {
		include $include_root."observation.php";
		continue;
	}
	
	// Eruption
	if ($wovoml_element['tag']=="ERUPTION") {
		include $include_root."eruption.php";
		continue;
	}
	
	// Phase
	if ($wovoml_element['tag']=="PHASE") {
		include $include_root."phase.php";
		continue;
	}
	
	// Video
	if ($wovoml_element['tag']=="VIDEO") {
		include $include_root."video.php";
		continue;
	}
	
	// Forecast
	if ($wovoml_element['tag']=="FORECAST") {
		include $include_root."forecast.php";
		continue;
	}
	
	// Inferred processes
	if ($wovoml_element['tag']=="INFERREDPROCESSES") {
		include $include_root."inferredprocesses.php";
		continue;
	}
	
	// Monitoring system
	if ($wovoml_element['tag']=="MONITORINGSYSTEM") {
		include $include_root."monitoringsystem.php";
		continue;
	}
	
	// Data
	if ($wovoml_element['tag']=="DATA") {
		include $include_root."data.php";
		continue;
	}
	
}*/
?>